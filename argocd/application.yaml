apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet #ArgoCD configuration definition file. Points ArgoCD to github repo as the source of truth to update existing eks cluster.
metadata:
  name: apps
  namespace: argocd
spec:
  generators:
    - git: #git generator to generate template parameters using the repo's folder structure. Utilized to create helm charts existing in multiple subfolders
        repoURL: https://github.com/RabeaKaddoura/coiner-full-devops-project.git
        revision: HEAD
        directories:
          - path: Helm-Charts/*
  template:
    metadata:
      name: "{{path.basename}}"

    spec:
      project: default
      source:
        repoURL: https://github.com/RabeaKaddoura/coiner-full-devops-project.git
        targetRevision: HEAD
        path: "{{path}}"
      destination:
        server: https://kubernetes.default.svc
        namespace: webapp

      syncPolicy:
        syncOptions:
          - CreateNamespace=true
        automated:
          prune: true
          selfHeal: true
