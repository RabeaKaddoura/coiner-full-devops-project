services:
  web:
    build: ./Frontend/Finances-App
    ports: 
      - 5173:5173
  api:
    build: ./Backend
    ports: 
      - 8000:8000
    volumes:
      - coiner_db:/app/data/
volumes:
  coiner_db: